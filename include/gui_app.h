/**
 * @file gui_app.h
 * @brief Main GUI application initialization and window event loop wrapper.
 */

#ifndef GUI_APP_H
#define GUI_APP_H

/**
 * @brief Starts the GUI application, initializes Raylib window, and runs the main loop.
 * @return 0 on clean exit; negative value on initialization failure.
 */
int GuiApp_Run(void);

#endif

